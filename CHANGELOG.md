### v5

- new `hollow` style: two-tone wireframe ring, transparent body, solid badges
- new `blade` style: long narrow arrow, hairline outline, sharp corners
- new `gruvbox` `solarized` `contrast` palettes, rm `nord` `ink`
- `sage` rebuilt: 19% lower chroma, hues evenly spaced, smooth value ramp
- macintosh badges are hand-drawn 1-bit bitmaps on square plates
- macintosh arrow, watch and magnifier snapped to the pixel grid
- diagonal resize dividers no longer read as swords
- margins now scale with outline weight; nothing clips its canvas
- contact sheets in the readme

### v4

- fix `light` + `dark` inversion
- rm `chonk` `material` `demo`
- `bubble` is a wider, shorter arrow instead of a gradient
- `ink` is paper-to-ink value ramp
- add wrist cuff to cartoony hands
- corner resize divider improvements
- arrowheads scale with stroke weight

### v3

- five new styles: `cartoony` `bubble` `macintosh` `material` `demo`
- new `ink` and `sage` palettes
- badge glyphs pick black or white per accent for legibility
- shadow, gloss, gradient and outline weight are now per-style
- previews organized into per-style folders

### v2

- arrowheads no longer punch a notch
- magnifiers tint lens
- corner resize cursors are actual arrows
- cell is a distinct thick plus
- crosshair center dot
- binary plists, quantized pngs
- new `chonk` style, new `latte` and `nord` palettes
